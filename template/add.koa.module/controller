import { Context } from 'koa'

/**
 * @api {post} /api/user/test 测试接口
 * @apiGroup user
 * @apiParam {string} phone 手机号
 */
export async function login (ctx: Context): Promise<void> {
  const { phone } = ctx.request.body

}
